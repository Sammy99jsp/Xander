We want the Action Space to depend on the AgentStats:

Guaranteed:
* End turn (!)
* Move (M.) for 8 directions { N, NE, E, SE, ... }

Then, for every Attack action, there exists:
* Attack (An.) for every target in range